import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {},
  msg: {
    fontSize: 16,
    color: 'red',
    // fontWeight: 'bold',
  },
  msgContainer: {
    height: 30,
    // padding: 10,
  },
});
