import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  row: {},
  labelContainer: {
    margin: 8,
  },
  label: {
    fontWeight: 'bold',
    fontSize: 18,
    color: 'black',
  },
  input: {
    borderWidth: 1,
    borderColor: '#d1d1d1',
    borderRadius: 5,
    padding: 8,
    fontSize: 16,
    marginBottom: 4,
  },
});
